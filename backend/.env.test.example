# Copy to .env.test and fill in a test-only database
# NEVER point this at your production DB!
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/meal_planner_test?schema=public"
JWT_SECRET="test-secret-not-for-production"
JWT_EXPIRES_IN="1d"
NODE_ENV="test"
